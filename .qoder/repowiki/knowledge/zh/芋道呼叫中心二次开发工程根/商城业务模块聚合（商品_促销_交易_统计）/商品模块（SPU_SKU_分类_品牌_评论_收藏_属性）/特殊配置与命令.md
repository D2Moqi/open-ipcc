启动类为 `ProductServerApplication`，默认端口 48100；通过 `application.yaml` 的 `spring.profiles.active=local` 加载 `application-local.yaml`，并从 Nacos 导入 `${spring.application.name}-${profile}.yaml` 配置；单元测试使用 `test/resources/sql/create_tables.sql` 与 `clean.sql` 初始化数据库。