启动类为 `PromotionServerApplication`，默认端口 48101；需确保 Nacos 中存在 `promotion-server-local.yaml` 或 `promotion-server-dev.yaml` 配置；测试脚本位于 `src/test/resources/sql/create_tables.sql` 与 `clean.sql`，用于初始化/清理营销相关表。