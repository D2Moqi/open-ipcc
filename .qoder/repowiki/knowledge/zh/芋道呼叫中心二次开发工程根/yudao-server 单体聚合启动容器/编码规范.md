- 新增业务模块时，先在 pom.xml 中以注释形式添加依赖占位，再按需取消注释启用，保持默认最小化编译产物。
- 对未启用的模块统一在 DefaultController 中提供 /admin-api/{module}/** 兜底接口，返回 CommonResult.error(NOT_IMPLEMENTED, ...) 并附带启用说明链接。
- 外部服务地址与密钥通过 application.yaml 中的环境变量占位符注入（如 ${OPENAI_API_KEY:sk-xxxx}），避免硬编码敏感信息。