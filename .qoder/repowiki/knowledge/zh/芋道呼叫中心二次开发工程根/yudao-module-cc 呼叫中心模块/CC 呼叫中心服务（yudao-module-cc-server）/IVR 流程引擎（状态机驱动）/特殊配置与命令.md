状态机运行依赖名为 `redisStateMachinePersister` 的 `RedisStateMachinePersister` Bean 以及 `stateMachineRedisTemplate`（键/值均使用 String/Jackson 序列化），需在启动前确保 Redis 可用；流程节点配置缓存 Key 模板为 `CALL_IVR_FLOW_INFO_NODE_KEY:{flowId}`，实例 Key 模板为 `CALL_IVR_INSTANCES_KEY:{instanceId}`，修改常量需同步更新所有引用。