Spring Boot + MyBatis-Plus（@TableName/@KeySequence）；FreeSWITCH 通过自定义 FsClient 以 ESL 协议发送 conference/originate/uuid_transfer 等命令；自动外呼使用 StringRedisTemplate 缓存任务上下文（TTL 24h），并通过 SipProxyGatewayService 经 SIP 代理出局或 loopback/public context 兜底。