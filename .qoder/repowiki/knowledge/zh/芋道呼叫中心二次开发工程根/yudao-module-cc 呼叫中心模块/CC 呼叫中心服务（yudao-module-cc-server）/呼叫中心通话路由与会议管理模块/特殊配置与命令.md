三方会议与自动外呼强依赖 FreeSWITCH ESL 连接（FsClient.getRandomAvailableAddress）与 SIP 代理地址（IpccSipProxyConfig.getSip().getPublicIp/Port），需确保 FS 已启用 conference/sofia/internal profile 并配置 public context 的 park extension；外部呼叫必须指定有效的 SipProxyGatewayDO 且状态为启用（status=0）。