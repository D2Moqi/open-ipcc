通过 `application.yml` 中 `cc.websocket.path`、`cc.websocket.sender-type`（local/redis/rabbitmq/rocketmq/kafka）、`cc.websocket.heartbeat-interval`、`cc.websocket.heartbeat-timeout` 控制行为；选择非 local 后端时需额外配置对应 MQ 的 topic/exchange（如 `cc.websocket.sender-kafka.topic`、`cc.websocket.sender-rabbitmq.exchange`）。