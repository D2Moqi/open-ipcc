模块 POM 不继承任何父 pom，完全独立发布；Spring Boot 自动配置通过 `src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` 注册；示例应用位于 `example/example-java/`，可通过其 `pom.xml` 单独编译运行 `ExampleJavaApplication` 或 `EslCommandDemoRunner`。