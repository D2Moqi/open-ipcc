通过配置项 `sipproxy.cluster.sender-type` 选择广播后端（local/redis/kafka/rocketmq/rabbitmq），默认 `local`；握手认证开关 `sipproxy.websocket.require-auth=false` 时允许无 token 调试模式；RFC 7118 子协议协商必须返回 `Sec-WebSocket-Protocol: sip`，否则 JsSIP 客户端会因 EOF 断开。