运行方式：example-1-java 需本地 Redis 可用；example-2-java 无需数据库；example-jssip 通过 `npm run dev` 启动于 http://localhost:5173；端到端测试先启动两个后端（8081/8082）及前端，再在 example/test 目录下执行 `python test_registration.py`，要求已安装 Playwright 浏览器。