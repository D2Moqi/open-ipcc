Vue 3 Composition API (`<script setup>`) + Element Plus（el-form/el-table/el-tabs/el-badge 等）+ TypeScript；HTTP 客户端统一来自 `@/config/axios`；字典显示使用 `@/utils/dict` 的 `DICT_TYPE` 与 `dict-tag`；表格日期列统一使用 `dateFormatter`；路由跳转使用 `vue-router` 的 `push`；标签页缓存使用 `useTagsViewStore`。