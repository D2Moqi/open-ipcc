- 自动配置类统一使用 `@AutoConfiguration` 注解并通过 `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` 注册，配合 `@ConditionalOnProperty` / `@ConditionalOnClass` 实现可选装配。
- 上下文信息通过 `*ContextHolder` 静态类暴露，内部使用 `TransmittableThreadLocal` 存储，并提供 `get/set/clear` 方法供 AOP/Web 层读写。
- 对 MyBatis Plus 插件的注册统一通过 `MyBatisUtils.addInterceptor(interceptor, inner, 0)` 插入到 `MybatisPlusInterceptor` 首位，保证在分页插件之前生效。
- AOP 切面与注解结合：通过自定义注解（如 `@DataPermission`、`@TenantIgnore`）标记目标方法，再由对应 Advisor/Aspect 读取注解并在执行前后操作上下文。
- 扩展点采用“接口 + 默认实现 + 工厂”模式（如 `DataPermissionRule`/`DataPermissionRuleFactoryImpl`、`TenantFrameworkService`/`TenantFrameworkServiceImpl`），便于使用者替换或追加规则。
- 工具类普遍使用 Lombok `@UtilityClass` + `@Slf4j` 组织静态方法，并以独立 util 包集中存放（如 `IPUtils`、`ExcelUtils`、`TenantUtils`）。