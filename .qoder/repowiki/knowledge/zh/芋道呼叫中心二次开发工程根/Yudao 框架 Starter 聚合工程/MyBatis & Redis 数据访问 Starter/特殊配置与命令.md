无需额外命令，引入对应 starter 后通过 `application.yml` 中的 `yudao.info.base-package`、`mybatis-plus.global-config.db-config.id-type`、`spring.datasource.druid.*`、`spring.cache.redis.*` 等属性即可生效；`yudao-spring-boot-starter-mybatis` 还附带《芋道 Spring Boot MyBatis 入门》《多数据源（读写分离）入门》《数据库连接池入门》三份文档。