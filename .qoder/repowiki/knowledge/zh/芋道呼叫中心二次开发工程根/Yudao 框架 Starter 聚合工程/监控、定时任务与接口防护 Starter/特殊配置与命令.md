每个 starter 通过 `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` 声明自动配置类，无需额外 XML 或 Java Config 即可被 Spring Boot 发现；monitor 需引入 `opentelemetry-api` 且默认受 `yudao.tracer.enable` 控制，job 需引入 `xxl-job-core` 且受 `xxl.job.enabled=true` 控制。