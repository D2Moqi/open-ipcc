启动需依赖 Nacos 提供 `gateway-server-{profile}.yaml` 配置；路由与 Knife4j 文档路由需在 `application.yaml` 中同步维护，新增下游服务时需同时添加 `spring.cloud.gateway.routes` 与 `knife4j.gateway.routes` 条目。构建产物由 `spring-boot-maven-plugin` repackage 生成可执行 jar。