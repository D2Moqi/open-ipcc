启动需先准备 Nacos（application.yaml 通过 optional:nacos:${spring.application.name}-${spring.profiles.active}.yaml 拉取远程配置）并设置 spring.profiles.active=local；单元测试依赖 test/resources/sql/create_tables.sql 建表与 application-unit-test.yaml 配置；验证码图片资源位于 resources/images/jigsaw 与 images/pic-click。