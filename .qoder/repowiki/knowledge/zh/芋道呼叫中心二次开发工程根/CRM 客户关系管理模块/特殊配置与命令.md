服务端口固定为 `48089`，默认激活 `local` profile，并通过 `spring.config.import` 同时加载本地 `application-{profile}.yaml` 与 Nacos 远端配置；启动类为 `cn.iocoder.yudao.module.crm.CrmServerApplication`，使用 `spring-boot-maven-plugin` 打包成 fat jar。