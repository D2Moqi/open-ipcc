启动类 `ImServerApplication` 默认加载 `application-local.yaml`，并通过 `spring.config.import` 同时引入 Nacos 远程配置；RTC 功能需额外部署 LiveKit 并配置 `yudao.im.rtc.livekitUrl/apiKey/apiSecret`，否则相关接口会抛 RTC_NOT_ENABLED；单元测试使用 `resources/sql/create_tables.sql` 建表、`application-unit-test.yaml` 隔离数据源。