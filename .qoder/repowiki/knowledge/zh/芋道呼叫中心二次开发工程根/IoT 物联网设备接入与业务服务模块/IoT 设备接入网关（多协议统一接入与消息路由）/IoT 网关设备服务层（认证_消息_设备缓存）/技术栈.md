Spring Boot + Lombok + SLF4J；JWT 使用 hutool-jwt；设备缓存使用 Guava `LoadingCache`（通过框架 `CacheUtils.buildAsyncReloadingCache` 构建）；远程调用使用 Spring `RestTemplateBuilder`；消息序列化通过 SPI 风格的 `IotMessageSerializerManager` 管理多种 `IotSerializeTypeEnum`。