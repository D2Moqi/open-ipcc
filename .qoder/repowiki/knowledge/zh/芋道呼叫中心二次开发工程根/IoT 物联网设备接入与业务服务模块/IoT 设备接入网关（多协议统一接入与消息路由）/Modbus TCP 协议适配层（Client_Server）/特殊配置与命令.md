协议启动时通过 `SpringUtil.getBean` 动态获取 `RedissonClient`、`IotDeviceCommonApi`、`IotDeviceMessageService`、`IotMessageBus` 等 Spring Bean，因此必须在已加载这些 Bean 的 Spring 容器中运行；Server 模式需确保配置的端口未被占用，否则 `netServer.listen()` 会抛出异常。