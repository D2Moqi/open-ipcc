- 新增协议需在 protocol 目录下按 {protocol}/Iot{Protocol}.java + Iot{Protocol}Config.java + handler/{upstream,downstream} 的固定结构组织，并由 IotProtocolManager 自动发现。
- 设备消息统一使用 core/topic 下的 *ReqDTO/*RespDTO 作为 MQ 传输载体，禁止在各协议中自定义消息格式。
- 消息总线实现通过实现 core/messagebus/core/IotMessageBus 接口并配合 AutoConfiguration 注入，支持本地/Kafka/RabbitMQ/Redis/RocketMQ 多后端切换。
- 跨模块共享常量集中在 api/enums 下按 alert/ota/product/rule/thingmodel 等域分包，避免硬编码字符串。