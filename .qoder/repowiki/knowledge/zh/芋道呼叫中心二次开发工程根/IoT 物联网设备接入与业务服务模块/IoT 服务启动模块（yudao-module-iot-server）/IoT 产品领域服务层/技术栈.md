Spring Boot + MyBatis-Plus（@TableName/@KeySequence/@TableId）持久化；使用框架内置的 PageResult/CommonResult/BeanUtils 统一分页、响应与对象转换；权限控制基于 Spring Security 的 @PreAuthorize 表达式；支持多租户（TenantBaseDO）；TDengine 表结构同步作为扩展能力暴露。