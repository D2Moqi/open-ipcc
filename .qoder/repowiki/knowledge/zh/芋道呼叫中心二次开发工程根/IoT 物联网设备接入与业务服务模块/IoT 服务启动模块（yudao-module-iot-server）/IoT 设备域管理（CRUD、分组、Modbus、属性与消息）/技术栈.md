Spring Boot + MyBatis-Plus（`@TableName`、`@KeySequence`、`LongSetTypeHandler`）、Lombok、Swagger v3（`@Tag`/`@Operation`）、Spring Security 方法级鉴权（`@PreAuthorize`）、Hutool 集合工具、芋道框架的 `CommonResult`/`PageResult`/`ExcelUtils`。