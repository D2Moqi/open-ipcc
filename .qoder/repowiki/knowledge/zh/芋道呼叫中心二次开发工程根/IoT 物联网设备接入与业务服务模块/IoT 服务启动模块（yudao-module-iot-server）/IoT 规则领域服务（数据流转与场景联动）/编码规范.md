- 每个规则类型均遵循 controller → service → (matcher/action) → dal 的分层结构，Controller 只处理 VO 转换与调用 Service。
- 可插拔的后端动作统一通过继承抽象基类或实现接口的方式扩展（如 IotAbstractDataSinkConfig、IotSceneRuleAction、IotDataRuleAction），新增目标只需新增配置类与 Action 实现。
- DO 与 VO 成对出现并按 data/scene 子目录隔离命名，避免两类规则的对象相互污染。