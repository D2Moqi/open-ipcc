服务默认端口 48088，启动时通过 `spring.config.import` 同时加载 `application-{profile}.yaml` 与 Nacos 中的 `${spring.application.name}-{profile}.yaml` 配置；需确保 `spring.profiles.active` 指向 local/dev 并配置好 MySQL、Redis、Nacos 地址。