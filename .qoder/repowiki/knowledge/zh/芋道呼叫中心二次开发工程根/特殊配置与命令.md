克隆后执行 `git submodule update --init --recursive` 拉取子模块；后端在 yudao-cloud 目录下用 `mvn spring-boot:run -pl yudao-server -Dspring-boot.run.profiles=local` 启动（端口 48080），前端在 yudao-ui-admin-vue3 目录下用 `pnpm dev` 开发并连接本地后端。