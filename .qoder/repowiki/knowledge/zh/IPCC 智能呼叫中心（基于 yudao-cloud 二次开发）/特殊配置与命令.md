克隆后需执行 `git submodule update --init --recursive` 拉取三个独立 Git 子仓库（主工程、前端、yudao-cloud 基座）；IDEA 需安装
Multi-Project Workspace 插件同时打开前后端。后端通过 `mvn clean install -pl yudao-server` 聚合编译，或单独进入
`yudao-module-cc/ipcc-sipproxy`、`ipcc-fs-esl` 构建自研通信组件。前端使用 `pnpm i` 安装依赖，`pnpm dev` 启动本地开发，
`pnpm build:prod` 构建生产包。CC 模块端到端测试位于 `yudao-module-cc/doc/test-all/`，需先安装 Python 虚拟环境与依赖（
`requirements.txt`），再运行 `cc_e2e_test.py`、`cc_concurrent_test.py` 等脚本。