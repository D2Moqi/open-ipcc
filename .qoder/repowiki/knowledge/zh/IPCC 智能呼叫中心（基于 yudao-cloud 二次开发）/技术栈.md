后端：Spring Boot 4.1 + Spring Cloud 2025.1 + Java 25 + Maven 多模块（flatten-maven-plugin 统一 revision）；ORM MyBatis +
Redis + Quartz；安全 Spring Security；RPC Feign。通信：自研 ipcc-sipproxy（SIP over WebSocket B2BUA）与 ipcc-fs-esl（Netty 4.x
FreeSWITCH ESL 客户端）。前端：Vue 3.5 + Vite 8 + Element Plus + TypeScript + Pinia + UnoCSS + LogicFlow/BPMN.js
可视化编排。构建与发布：Dockerfile 每个 server 独立镜像，CI 由 GitHub Actions 驱动。