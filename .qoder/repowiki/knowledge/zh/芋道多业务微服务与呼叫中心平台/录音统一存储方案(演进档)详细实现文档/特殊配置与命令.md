阶段 0 需在测试 FS 上执行 T1-T6 探针用例 (同 uuid 并发双 fork、fork 与 record_session 并存、stereo 声道归属、8k/16k
帧节奏等),产出判定后再冻结架构;生产部署需挂载持久 spoolDir (`/data/cc-record-spool`),并在 application-{local,prod}.yaml
中新增 `cc.recording.*` 段 (默认全关)。